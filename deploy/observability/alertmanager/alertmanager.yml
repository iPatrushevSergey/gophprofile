route:
  receiver: default
  group_by: [alertname, job, service_name]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: default
    webhook_configs:
      - url: http://alert-webhook/alerts
        send_resolved: true
