# Copy to repo root as .env.vault. Used by: make vault-seed
VAULT_TOKEN=root

POSTGRES_PASSWORD=gophprofile
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
RABBITMQ_PASSWORD=gophprofile
GRAFANA_ADMIN_PASSWORD=admin
